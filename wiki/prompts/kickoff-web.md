# Kickoff prompt — DBRealtorWeb portal session

> Use this at the start of a new Claude Code session on the web portal project.

---

We are working on the DBRealtor monorepo. The web portal is in `/DBRealtorWeb/`.

Before writing any code:

1. **Read** `CLAUDE.md` (root), `DBRealtorWeb/CLAUDE.md`, `wiki/architecture/web.md`.
2. **Read** `DBRealtorWeb/docs/progress.md` for current status.
3. Remember: this project is **read-only**. No INSERT/UPDATE/DELETE. No `session.commit()`.

Then tell me the current state of the portal and what's ready to work on next.

---
